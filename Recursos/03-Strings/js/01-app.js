// Declaración de una cadena de texto utilizando comillas dobles y escapando la comilla doble dentro de la cadena
const producto = "Monitor 20\""; 

// Declaración de una cadena de texto utilizando String
const producto2 = String('Monitor 24 Pulgadas');

// Declaración de una cadena de texto utilizando new String
const producto3 = new String('monitor 27 pulgadas');

// Imprimir las cadenas de texto en la consola
console.log(producto);
console.log(producto2);