const nombre = "Mionitor 20 Pulgadas";
const precio = 500;
const disponible = true;
const producto = {
    nombre: "Monitor 20 pulgadas",
    precio: 300,
    disponible: true
}

console.log(producto);

console.log(producto.nombre);
Console.log(producto.precio);
console.log(producto.disponible);

console.log(producto.['nombre']);
